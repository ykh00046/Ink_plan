{
  "name": "ink-plan",
  "version": "1.0.0",
  "private": true,
  "description": "잉크/사출 생산계획 시스템 — data-service 순수 로직 단위 테스트",
  "scripts": {
    "test": "node --test tests/data-service.test.js tests/ui-regressions.test.js tests/date-utils.test.js tests/extracted-logic.test.js tests/history-snapshot-diff.test.js",
    "test:py": "python -m unittest discover -s tests -p \"*_test.py\""
  }
}
